<h2>Share</h2>
<div>
  <p>
  This share is <i>{{ status }}</i> : 
  {{#if isActive }}
    {{#if isMine}}
    <button {{ action "withdraw" }}>Withdraw</button>
    {{else}}
      {{#if isAccepted }}
        <button {{ action "revoke" }}>Revoke</button>
      {{else}}
        <button {{ action "accept" }}>Accept</button>
        <button {{ action "reject" }}>Reject</button>
      {{/if}}
    {{/if}}
  {{/if}}
  </p>
</div>

<table class="table">
  <tr>
    <th>
      From
    </th>
    <th>
      To
    </th>
    <th>
      Sensors
    </th>
  </tr>
  <tr>
    <td>
      {{requester.email}}
    </td>
    <td>
      {{requestee.email}}
    </td>
    <td>
      <ul>
        {{#each sensor in sensors}}
        <li>{{sensor}}</li>
        {{/each}}
    </td>
  </tr>
</table>

<hr>

<h4>Message</h4>

{{ message }}

{{ outlet }}
